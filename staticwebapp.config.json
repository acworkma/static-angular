{
    "routes": [
        {
            "route": "/memberwebapp",
            "rewrite": "/memberwebapp/index.html"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}